# Demo

## From PowerShell

```powershell
Invoke-RestMethod -Method Post -Uri "http://localhost:8000/demo/run"
```

## From the dashboard

Open:

```text
http://localhost:8501
```

Then:

```text
Analizar → Ejecutar demo
```

The demo creates a case report and an executive report. Both can be opened from the dashboard.
